# 安全修复构建错误（仅限一处删除）

## 用户已确认的范围
- 后端与环境变量：完全不动。不修改、不重新生成、不重新连接、不切换 `.env` 与云端后端。环境变量问题下一步单独处理。
- 只修复一个构建错误：`src/data/greensprout.ts` 第 641 行末尾混入了一行孤立的 Markdown 代码围栏符号（```），导致 TS1160 和 Vite 解析错误，`build:dev` 失败。

## 执行步骤
1. 删除 `src/data/greensprout.ts` 末尾第 641 行的孤立 ``` 行。除这一行外，不修改该文件任何内容；不修改任何业务逻辑、UI、数据库结构或后端配置。
2. 运行 `bunx tsgo --noEmit`，报告结果。
3. 触发一次构建验证（`build:dev`），报告结果。
4. 完成后停止，等待下一步指示。

## 明确不做的事
- 不修改 `.env`，不切换或重连后端。
- 不处理环境变量归属问题（单独处理）。
